#include "main.h"

/**
 * mul - multiplies two intergers
 * @x: the first numeber to multiply
 * @y: he second numebre to multiply
 * Return: The results between multiplication between x pram and y pram
 */

int mul(int x, int y)
{
	return (x * y);
}

