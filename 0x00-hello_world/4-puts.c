#include <stdio.h>
/**
 * main - A proram that prints a line with the put function
 * Return:0 (succes)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
